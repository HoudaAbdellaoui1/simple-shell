#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char *message = "Hello, Betty!";

    printf("%s\n", message);

    return (0);
}
